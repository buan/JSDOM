Change content in a webpage:Before that we need to know that query selector elements are not HTML collection by default.They are NodeList by default.For elements in NodeList we dont need to convert it into array.We can directly loop through the elements by forEaach method
